# CI du générateur et de la collection. Aucun identifiant Exoscale n'est
# nécessaire : le contrat est versionné, et rien ne va sur le réseau.
#
# Les étapes appellent les tâches de `mise.toml` plutôt que de recopier leurs
# commandes : une CI qui diverge du lanceur ne mesure pas ce qu'on croit.
#
# Trois règles tiennent la sécurité de ce fichier, et elles valent pour tous
# les workflows du dépôt :
#
# 1. **Aucun droit par défaut.** `permissions: {}` au niveau du workflow, puis
#    chaque job déclare le strict nécessaire. Un job qui n'a besoin de rien
#    n'obtient rien.
# 2. **Aucune action par tag.** Un tag est mobile : `@v4` est du code qui peut
#    changer sans revue, et c'est exactement le scénario tj-actions de mars
#    2025. Tout est épinglé par SHA de commit, avec la version en commentaire
#    pour que quarante caractères restent lisibles.
# 3. **Aucun jeton laissé derrière.** `persist-credentials: false` sur chaque
#    checkout : sans lui, `actions/checkout` laisse le `GITHUB_TOKEN` dans
#    `.git/config`, à portée de tout ce que la suite du job exécute.
name: ci

on:
  push:
    branches: [main]
  pull_request:
  # La CI tourne aussi sans changement : une dépendance qui casse toute seule
  # ne se voit pas autrement.
  schedule:
    - cron: "0 4 * * 1"

permissions: {}

# Une pull request repoussée annule la série précédente. Sur `main`, rien n'est
# annulé : ce qui a été poussé doit avoir été mesuré.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  generator:
    name: Générateur
    # Image épinglée plutôt que `ubuntu-latest` : une CI dont le socle change
    # sous elle ne dit pas si une régression vient du code ou de la machine.
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Durcir le runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # `audit` et non `block` : la politique se mesure avant de se
          # fermer. Une allowlist écrite sans avoir observé le trafic réel
          # casse la CI sans rien prouver.
          egress-policy: audit

      - name: Récupérer le dépôt
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # `version` épingle mise lui-même, sans quoi l'action télécharge la
      # dernière release publiée : mise installe Python et ruff et lance toutes
      # les tâches, donc le laisser flotter annulerait l'épinglage du reste.
      - name: Installer mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          version: 2026.2.23

      - name: Installer
        run: mise run setup

      - name: Style
        run: mise run lint

      - name: Types
        run: mise run typecheck

      - name: Tests
        run: mise run test

      # Recense le document entier, puis sort en code 2 sur toute opération
      # non classée ou tout override orphelin d'un produit indexé.
      - name: Rapport de couverture
        run: mise run report

      # Les artefacts versionnés doivent être ceux que le générateur produit,
      # golden de l'IR, modules rendus et modules de la collection compris.
      - name: Dérive des artefacts générés
        run: mise run check:generated

      - name: Fragments de changelog
        run: mise run lint:changelog

      # Une garde dont la suppression laisse les tests verts est un commentaire.
      - name: Falsification des gardes
        run: mise run falsify

      # Les compteurs des deux README sont dérivés : un nombre qui a vieilli
      # rougit ici plutôt que de se lire comme une mesure.
      - name: Compteurs des README
        run: mise run readme:check

      - name: Publier les rapports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: rapports
          path: build/reports/
          if-no-files-found: warn

  collection:
    # Ce qu'Ansible dit du fichier produit : documentation, types, en-têtes,
    # import. Le travail du générateur n'est pas jugé par le générateur.
    #
    # La matrice couvre chaque version d'ansible-core que `meta/runtime.yml`
    # déclare supporter, et la borne basse est **mesurée** : le 4 septembre
    # 2026, `ansible-test sanity` a été lancé sur cette collection de 2.16 à
    # 2.21. 2.16 échoue sur deux contrôles de boilerplate Python 2 que ces
    # modules ne peuvent pas satisfaire ; 2.17 à 2.21 passent les mêmes 24
    # contrôles. La matrice n'est pas recopiée de collection-scaleway : elle
    # va jusqu'à 2.21 parce que 2.21 a été mesuré.
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    name: collection (ansible-core ${{ matrix.ansible }})
    strategy:
      fail-fast: false
      matrix:
        ansible:
          - ">=2.17,<2.18"
          - ">=2.18,<2.19"
          - ">=2.19,<2.20"
          - ">=2.20,<2.21"
          - ">=2.21,<2.22"
    steps:
      - name: Durcir le runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit

      - name: Récupérer le dépôt
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Installer mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          version: 2026.2.23

      - name: Installer
        run: mise run setup

      # La valeur de matrice passe par `env:` et non par une interpolation dans
      # le `run:` : une donnée interpolée directement dans un shell est une
      # injection de template, et c'est le premier défaut que zizmor cherche.
      - name: Épingler ansible-core
        env:
          ANSIBLE_CORE: ${{ matrix.ansible }}
        run: mise run setup:ansible

      # Le script refuse un vert obtenu sur zéro fichier examiné : sous une
      # arborescence que git ne suit pas, ansible-test saute toutes ses cibles
      # et sort en 0.
      - name: ansible-test sanity
        run: mise run sanity

      # L'outil officiel de documentation voit ce que sanity laisse passer :
      # sur une référence croisée invalide, sanity sort en 0 et lui en 3.
      - name: Documentation de la collection
        run: mise run lint:docs

  package:
    # L'archive installable, et la preuve qu'elle sert : contenu contrôlé
    # contre ce que `plugins/` porte sur le disque, installation réelle, puis
    # `ansible-doc` interrogé depuis la collection installée.
    name: Archive
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - name: Durcir le runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          egress-policy: audit

      - name: Récupérer le dépôt
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Installer mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          version: 2026.2.23

      - name: Installer
        run: mise run setup

      - name: Construire et vérifier l'archive
        run: mise run package

      # Ce que les tâches construisent doit être ignoré par git. Un artefact
      # qui apparaît ici est un fichier que quelqu'un commitera sans le vouloir.
      - name: L'arbre de travail est resté propre
        run: mise run check:worktree

      - name: Publier l'archive
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: collection
          path: build/dist/*.tar.gz
          if-no-files-found: error

  exemple:
    # La plateforme d'exemple, contre l'émulateur : la stack Terraform
    # s'applique, l'inventaire dynamique retrouve ce qu'elle a déployé, le
    # playbook appelle chaque module qui a une cible, puis tout est détruit et
    # le contrôle de résidu compare l'émulateur avant et après. C'est la seule
    # preuve de comportement du dépôt, et c'est elle qui a trouvé une adresse
    # orpheline et une contrainte du TPM avant tout run réel.
    #
    # **La cible réelle n'est pas ici et ne le sera pas.** Elle crée des
    # ressources facturées sur le compte du mainteneur, et ne se lance qu'avec
    # son accord, demandé à chaque fois.
    #
    # Ce que ce job ne prouve pas : ce que feint décline (VPC, console, mot de
    # passe, DNS inverse, export d'instantané, et sept produits entiers). Le
    # playbook le tolère contre l'émulateur seul et le recense ; l'artefact
    # publié dit ce qui a réellement tourné.
    name: Plateforme d'exemple
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Durcir le runner
        uses: step-security/harden-runner@05e31511f85b41b11d1cf0ef85d0992719546e2c # v2.21.0
        with:
          # Terraform télécharge son fournisseur, et le job construit
          # l'émulateur depuis le proxy des modules Go : `audit` documente ce
          # trafic au lieu de le deviner.
          egress-policy: audit

      - name: Récupérer le dépôt
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # mise installe aussi Terraform, épinglé dans `mise.toml` comme le reste.
      - name: Installer mise
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          version: 2026.2.23

      - name: Installer
        run: mise run setup

      # **Construit, pas téléchargé, et c'est provisoire.** La release 0.12.1
      # de feint refuse encore le fournisseur Terraform d'Exoscale par user
      # agent ; le refus est levé sur `main` au commit 9074c0f, sur une mesure.
      # Tant qu'aucune release ne le porte, la tâche `feint:install` construit
      # ce commit-là, épinglé par SHA, avec la commande que le poste utilise.
      # Le jour où une release le porte, revenir à `stephrobert/setup-feint`
      # épinglé par SHA, qui contrôle la somme du binaire, et retirer Go d'ici.
      # `cache: false` : sans `go.sum` dans ce dépôt, il n'y a rien à mettre
      # en cache, et l'action le dirait à chaque run.
      - name: Installer Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: "1.26.x"
          cache: false

      - name: Construire l'émulateur
        run: mise run feint:install

      # Le lanceur démarre feint lui-même, sur le port que `scripts/example.py`
      # attend, qui n'est ni le 4599 par défaut ni le 4877 de
      # collection-scaleway, et l'arrête à la fin puisqu'il l'a démarré.
      - name: La plateforme d'exemple se déploie, s'exploite et se détruit
        run: mise run example

      - name: Publier l'artefact de couverture
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: exemple
          path: build/example/*.json
          if-no-files-found: warn
