{
  "product": "quotas",
  "version": "v2",
  "title": "Exoscale API",
  "description": "Infrastructure automation API, allowing programmatic access to all Exoscale products and services.",
  "source": "exoscale.v2.json",
  "zones": [
    "ch-gva-2",
    "ch-dk-2",
    "de-fra-1",
    "de-muc-1",
    "at-vie-1",
    "at-vie-2",
    "bg-sof-1",
    "hr-zag-1"
  ],
  "operations": [
    {
      "key": "quotas.v2.Quota.get-quota",
      "id": "get-quota",
      "product": "quotas",
      "version": "v2",
      "resource": "quota",
      "http_method": "GET",
      "path": "/quota/{entity}",
      "summary": "Retrieve Resource Quota",
      "tags": [
        "quotas"
      ],
      "parameters": [
        {
          "name": "entity",
          "type": "string",
          "required": true,
          "location": "path"
        }
      ],
      "response": {
        "schema": "quota",
        "payload_schema": "quota"
      }
    },
    {
      "key": "quotas.v2.Quota.list-quotas",
      "id": "list-quotas",
      "product": "quotas",
      "version": "v2",
      "resource": "quota",
      "http_method": "GET",
      "path": "/quota",
      "summary": "List Organization Quotas",
      "tags": [
        "quotas",
        "ccm"
      ],
      "response": {
        "payload_field": "quotas",
        "payload_schema": "quota",
        "is_list": true
      }
    }
  ],
  "warnings": [
    "aucune opération ne déclare de paramètre de pagination (page, limit, offset, cursor) : une liste est rendue en une réponse, et le contrat ne promet pas qu'elle soit complète"
  ]
}
